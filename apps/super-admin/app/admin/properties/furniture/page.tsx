import { PropertyTable } from "@/components/properties/PropertyTable";

export default function FurniturePropertiesPage() {
  return (
    <PropertyTable
      category="furniture"
      title="Furniture Ecosystem"
      description="Packaged workstation setups and individual office furniture assets — a separate asset ecosystem, not a residential/commercial subtype."
    />
  );
}

version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: zero_brokerage_redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
    name: zero_brokerage_redis_data
